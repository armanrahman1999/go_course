package basics

import "fmt"

var kasta string = "Kasta"

func main() {	
	var name string = "Kawai"
	name = "Chodu"
	age := 10
	kasta:= "is a " + kasta
	fmt.Println(age)
	fmt.Println(name)
	fmt.Println(kasta)
}